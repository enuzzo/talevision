# Copy to secrets.yaml and fill in values. Never commit secrets.yaml.
# Generate bcrypt hash: python -c "import bcrypt; print(bcrypt.hashpw(b'yourpassword', bcrypt.gensalt()).decode())"
web_auth:
  username: "admin"
  password_hash: "<BCRYPT_HASH>"
webhook_url: null
weather_api_key: null
# Koan mode: Groq (preferred) or Gemini API key for haiku generation
# Groq free tier: console.groq.com — 500K tokens/day
# Gemini free tier: aistudio.google.com — 1M tokens/day
groq_api_key: "<GROQ_API_KEY>"
# gemini_api_key: "<GEMINI_API_KEY>"
